version: 2
jobs:
  build:
    docker:
      - image: docker:20.10.23

    steps:
      - checkout
      - setup_remote_docker
      
      - run:
          name: Install dependencies
          command: |
            apk add --no-cache py-pip
            pip install docker-compose==1.29.0

      - run:
          name: Start container
          environment:
            DATABASE_CLIENT: postgres
            DATABASE_HOST: 127.0.0.1
            DATABASE_PORT: 5432
            DATABASE_NAME: f1_league_admin
            DATABASE_SSL: 'false'
            DATABASE_USERNAME: strapi
            DATABASE_PASSWORD: strapi
            NODE_ENV: development
            JWT_SECRET: 5xxPspalhhtHuLcDhl+wBA==
            ADMIN_JWT_SECRET: FByTpKS4H8neKhvZEspoKQ==
          command: |
            docker-compose up